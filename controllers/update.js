const Hub = require('../models/hub')
const Update = require('../models/update')

/*
 * GET /api/updates/:hubID
 * Create a new update
 * 200 Res: {message: String, objects: Updates}
 * 400 Res: {message: String}
 */
 exports.getUpdates = function(req,res){}
